# 数独项目 · 需求说明书

## 1. 项目概述

Tauri v2 桌面应用，Rust 后端处理数独引擎，React + TypeScript 前端渲染 UI。

| 维度 | 选择 |
|------|------|
| 框架 | Tauri v2 |
| 后端语言 | Rust |
| 前端 | React 18 + TypeScript |
| 构建工具 | Vite 6 |
| 目标平台 | Windows (优先), macOS, Linux |

---

## 2. 功能需求

### 2.1 核心游戏功能

- **棋盘渲染**：9×9 标准数独网格，每个格子可填入 1-9
- **数字输入**：点击格子后选择数字（键盘 + 屏幕数字面板）
- **原题锁定**：初始给出的数字不可修改，视觉上区分（灰色/加粗）
- **冲突检测**：同一行/列/宫出现重复数字时，高亮冲突的格子（红色）
- **同数高亮**：选中某个数字时，棋盘上所有相同数字高亮
- **完成检测**：所有格子填完且无冲突时，触发完成庆祝动画

### 2.2 数独生成器

- **算法**：填空法（先填满对角线 3 个 3×3 宫，再回溯求解得到完整盘，然后挖空）
- **难度分级**：

| 难度 | 挖空数 | 策略要求 |
|------|--------|---------|
| 简单 (Easy) | 30-35 | 仅需唯一余数/摒除 |
| 中等 (Medium) | 36-45 | 需区块摒除/数对 |
| 困难 (Hard) | 46-52 | 需 X-Wing / 链 |
| 专家 (Expert) | 53-58 | 需多种高级技巧 |

- **唯一解保证**：生成的谜题有且仅有一个解
- 生成策略：用挖洞法，每挖一个洞检查解是否唯一

### 2.3 数独求解器

- **核心算法**：回溯 (backtracking) + 约束传播
- **优化**：优先填入候选数最少的格子 (MRV)
- **输入**：接收当前棋盘状态（含用户已填数字）
- **输出**：完整解，或无解提示

### 2.4 提示系统

- **下一个提示**：在用户当前棋盘基础上，计算一个格子的正确数字
- **高亮显示**：被提示的格子闪烁/高亮，自动填入
- **提示次数**：可限制或不限（可配置）

### 2.5 计时器

- **正计时**：从游戏开始累计时间，显示 分:秒
- **暂停/继续**：可选实现
- **计时统计**：完成时记录总用时

### 2.6 游戏控制

- 新游戏（保持当前难度）
- 难度切换 → 自动开始新游戏
- 检查（高亮当前所有错误填写的格子）
- 重置（清空所有用户填入的数字，保留原题）
- 擦除（清除当前选定格子的数字）
- 笔记模式（可选，填入候选数字标记）

---

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────┐
│           Frontend (React)           │
│  Board · Cell · Controls · Timer     │
│  useSudoku (状态管理)                │
├────────────── Tauri IPC ─────────────┤
│  invoke()  ←→  #[tauri::command]     │
│           Rust Backend               │
│  generator · solver · validator      │
│  board · difficulty                   │
└─────────────────────────────────────┘
```

### 3.2 Rust 后端模块划分

```
src-tauri/src/
├── main.rs            # 入口
├── lib.rs             # Tauri 插件注册
├── commands.rs        # Tauri IPC 命令层
└── sudoku/
    ├── mod.rs         # 模块导出
    ├── board.rs       # Board 数据结构
    ├── validator.rs   # 校验器（行/列/宫检查）
    ├── solver.rs      # 回溯求解器
    └── generator.rs   # 谜题生成器 + 难度控制
```

#### Board 数据结构

```rust
struct Board {
    cells: [[Option<u8>; 9]; 9],  // 0 = 空, 1-9 = 数字
}
struct Puzzle {
    puzzle: Board,   // 挖空后的题目（用户看到）
    solution: Board, // 完整解（用于校验/提示）
}
```

#### Tauri IPC 命令

| 命令 | 参数 | 返回 | 说明 |
|------|------|------|------|
| `generate_puzzle` | difficulty: String | Puzzle | 生成新谜题 |
| `check_cell` | board, row, col, num | bool | 检查填入是否正确 |
| `solve_board` | board | Board | 求解当前棋盘 |
| `get_hint` | board, solution | Hint (row, col, num) | 给出一个提示 |
| `validate_board` | board | Vec<Conflict> | 返回所有冲突位置 |

### 3.3 前端组件树

```
App
├── Header
│   ├── Title ("数独")
│   └── Timer
├── DifficultySelector (Easy / Medium / Hard / Expert)
├── Board
│   └── Cell × 81 (每个格子)
│       ├── value (数字)
│       ├── isGiven (原题)
│       ├── isSelected
│       ├── isHighlighted (同数高亮)
│       ├── isConflict (冲突)
│       └── isHinted (提示)
├── NumberPad (1-9 按钮 + 擦除)
└── Controls
    ├── NewGame
    ├── Check
    ├── Hint
    ├── Reset
    └── Undo (可选)
```

### 3.4 状态管理（useSudoku hook）

```
状态:
- board: number[][]          当前棋盘 9×9
- solution: number[][]       完整解
- given: boolean[][]         是否为原题数字
- selectedCell: [row, col] | null
- difficulty: Difficulty
- timer: number (秒)
- gameStatus: 'idle' | 'playing' | 'paused' | 'completed'
- conflicts: [row, col][]    冲突位置列表
- hintCell: [row, col] | null
- mistakes: number           错误次数
```

---

## 4. UI/UX 设计思路

### 4.1 视觉风格

- 极简清爽，白底灰线
- 3×3 宫格用粗边框分隔
- 原题数字：深色，粗体
- 用户填入数字：蓝色，正常字重
- 冲突数字：红色背景
- 选中格子：蓝色高亮
- 同数高亮：浅黄色背景
- 提示格子：绿色短暂闪烁

### 4.2 布局

```
┌────────────────────────┐
│  数独         00:42    │  ← Header
│  ○ Easy ○ Medium ● Hard│  ← Difficulty
│                        │
│  ┌───┬───┬───┐        │
│  │ 5 │ 3 │   │        │  ← Board (粗线分宫)
│  ├───┼───┼───┤        │
│  │ 6 │   │   │        │
│  ├───┼───┼───┤        │
│  │   │ 9 │ 8 │        │
│  └───┴───┴───┘        │
│  1 2 3  4 5 6  7 8 9  │  ← NumberPad
│  [Erase]              │
│                        │
│  [New] [Check] [Hint]  │  ← Controls
└────────────────────────┘
```

---

## 5. 数据流

### 开始新游戏
```
用户选择难度 → invoke('generate_puzzle', { difficulty })
              → Rust: generator 生成 puzzle + solution
              → 返回给前端: { puzzle, solution }
              → 前端渲染棋盘，启动计时器
```

### 用户填入数字
```
用户点击格子 → 选择数字
            → 前端更新 board[row][col] = num
            → 前端检测: 是否冲突（本地校验）
            → 渲染冲突高亮
            → 检测: 是否完成
```

### 用户请求提示
```
用户点击 Hint → invoke('get_hint', { board, solution })
              → Rust: 找一个空位/错误位返回 {row, col, num}
              → 前端闪烁高亮提示格子
              → 自动填入
```

---

## 6. 生成器算法细节

### 步骤
1. **生成完整解**：
   - 先填满 3 个对角线 3×3 宫（随机排列 1-9）
   - 用回溯求解器填充剩余格子
   - 得到一个随机完整终盘

2. **挖洞**：
   - 从 81 个格子中随机选一个挖掉
   - 用求解器检查是否仍有唯一解
   - 如果有唯一解则保留空洞，否则放回
   - 重复直到挖出足够的空格数

### 难度控制
- 挖空数决定基础难度
- 简单难度多用唯一余数可解，困难需要高级技巧
- 通过控制挖空模式和位置进一步微调

---

## 7. 阶段规划

### Phase 1 — MVP
- 完成 Rust 后端：Board、Validator、Solver、Generator
- 完成 Tauri IPC 接口
- 完成前端基础 UI：棋盘渲染、数字输入、原题锁定
- 实现 New Game、难度选择

### Phase 2 — 完善功能
- 提示系统
- 冲突高亮
- 同数高亮
- 计时器
- 检查功能

### Phase 3 — 体验优化
- UI 润色
- 完成动画
- 响应式适配
- 键盘快捷键支持
- 笔记/候选标记

---

## 8. 开放性讨论点

- [ ] **候选笔记模式**：是否支持用户在格子里标记多个候选数字（类似铅笔笔记号）
- [ ] **撤销功能**：是否支持多步撤销
- [ ] **错误计数**：是否统计错误填入次数
- [ ] **暂停**：是否需要暂停游戏/计时
- [ ] **键盘快捷键**：方向键移动 + 数字键输入
- [ ] **自动填笔记**：是否自动计算并显示每个空格的候选数
- [ ] **音效**：操作是否有音效反馈
- [ ] **保存进度**：关闭应用后恢复上次游戏

---

**请确认以上设计，或提出修改意见。确认后开始实施。**